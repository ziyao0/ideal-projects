server:
  port: 10002

spring:
  application:
    name: ideal-cloud-uaa
  profiles:
    active: register
  jmx:
    enabled: false
  main:
    allow-bean-definition-overriding: true

    allow-circular-references: true