server:
  port: 10002
spring:
  application:
    name: user-center
  profiles:
    active: register
  jmx:
    enabled: false
  main:
    allow-bean-definition-overriding: true

    allow-circular-references: true