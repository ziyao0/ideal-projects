spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
config:
  gateway:
    skip-apis:
      - /user-center/usercenter/app/list
      - /user-center/auth/login
